// Shared domain types — canonical import point for all features
// Usage: import type { Invoice, Customer, Product } from '@/shared/types';

export type {
  Invoice,
  Customer,
  Product,
  BillingItem,
  PaymentMode,
  PaymentSplit,
  CreateInvoicePayload,
} from "@execora/shared";
